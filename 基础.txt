1.字符、字符数组与字符串
  字符串实际上是以null'\0'终止的字符数组
  C++ 中有大量的函数用来操作以 null 结尾的字符串：
  （1）strcpy(s1,s2)=>复制字符串s2到s1；
  （2）strcat(s1,s2)=>连接字符串s2到s1的末尾；
  （3）strlen(s1)=>返回字符串s1的长度；
  （4）strcmp(s1,s2)=>返回s1与s2比较的结果；
  （5）strchr(s1,ch)=>返回一个指针，指向字符串s1中字符ch第一次出现的位置；
  （6）strstr(s1,s2)=>返回一个指针，指向字符串s1中第一西出现s2的位置；